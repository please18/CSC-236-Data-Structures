class BstNode():
    def __init__(self):
        self.data = []
        self.BstNode = (left, right)
        self.head = 0
        self.newNode = 0

    def GetNewNode(self):

        BstNode = newNode
        newNode = new_BstNode
        newNode = data
        newNode.left = newNode
        return newNode

    def BstNode():
        insert(BstNode.head, data)
        if head == 0:
            head = GetNewNode(data)
        elif data <= head.data:
            head = insert(head.left, data)
        else:
            head.right = insert(head.right, data)
        return head


def main():
    BstNode.head = 0
    head = insert(head, 6)
    head = insert(head, 4)
    head = insert(head, 7)
    head = insert(head, 10)

main()